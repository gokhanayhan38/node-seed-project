{
  "name": "components",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "dependencies": {
    "action-defs": "file:../action-defs",
    "classnames": "2.2.3",
    "autoincrement": "1.0.0",
    "format-number": "2.0.1",
    "underscore": "1.8.3",
    "react-data-grid": "1.0.49",
    "faker": "3.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-plugin-transform-react-jsx": "^6.8.0"
  },
  "script": {
    "prepare": "rm -rf build && mkdir build && cp index.js build && babel --plugins transform-react-jsx src --out-dir build/src"
  }
}
